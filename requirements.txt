# The real dependencies are declared in pyproject.toml, which is the single
# source of truth. This file exists so the command in the README keeps working;
# it installs the project in editable mode with its development extras.
-e .[dev]
